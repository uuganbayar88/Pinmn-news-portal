# Explicit fixture mode for local development. Use api when the backend is ready.
CONTENT_SOURCE=fixtures
# Server-only URL; never add a NEXT_PUBLIC prefix to backend secrets.
CONTENT_API_URL=http://localhost:4000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Set true only after production content and indexing are approved.
ALLOW_INDEXING=false
